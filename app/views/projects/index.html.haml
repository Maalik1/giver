.header
  %h1 Browse Projects

- @projects.each_slice(4) do |slice|
  .row
    - slice.each do |project|
      .col-sm-3
        .module
          .section.full
            = image_tag project.photo.url
          .section
            %h4.top= link_to project.title, project
            %p= project.blurb[0..85] + '...'
            %small= link_to project.org.name, project.org