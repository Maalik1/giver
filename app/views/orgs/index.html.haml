.header
  %h1 Organizations

- @orgs.each_slice(4) do |slice|
  .row
    - slice.each do |org|
      .col-sm-3
        .module
          .section
            %h4.top= link_to org.name, org
            %p= org.description[0..85] + '...'